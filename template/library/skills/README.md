# Workspace workflows

Each workflow lives in its own directory with a SKILL.md. Codex discovers these through relative links in the workspace's `.agents/skills/`. In Cursor, reference the appropriate SKILL.md explicitly, or copy a focused instruction to a Cursor rule if it should apply automatically.
