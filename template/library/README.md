# Reusable library

`templates/` contains project starting points. `skills/` contains shared agent workflows. `tools/` contains scripts. Copy template material into each project; do not import from this directory at runtime. Record lessons that apply across projects in `../agent-docs/`.
